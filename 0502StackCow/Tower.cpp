#include<iostream>
#include<stack>
#include<string>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	long long int num;
	cin >> num;
	stack<int> s;
	string tower;
	for (int i = 0; i < num; i++)
	{
		cin >> tower;

	}
}